# Hand-off note to GPU operator (2026-05-19 21:00)

## Where we are right now

- **GPU node**: Qwen-2.5-14B has already produced 1620/1800 trajectories
  across 9 cells (GSM8K/Math/HotpotQA × react/cot/direct), pushed in
  commit `c1f89eb`.
- **Control PC** has just pushed commit `f1a0d7a` adding a focused
  **mechanism pilot** based on first-token logprob KL divergence.

## What the GPU operator should do next (one shot, ~5 minutes)

This assumes a vllm server is still serving Qwen-2.5-14B somewhere
(port 8000 or 8001). If both servers are still up, port 8000 is fine.

```bash
cd /data/agentdiff-emnlp        # or wherever the repo lives on the GPU node
git pull                          # picks up gpu_kit/probe_decoding_kl.py

# (a) sanity probe — does the vllm server still serve top_logprobs?
source gpu_kit/.venv/bin/activate
python3 - <<'PY'
import requests, json
r = requests.post("http://localhost:8000/v1/chat/completions",
    json={"model":"qwen2.5-14b",
          "messages":[{"role":"user","content":"What is 2+2?"}],
          "max_tokens":1,"logprobs":True,"top_logprobs":5},
    timeout=60)
print("status:", r.status_code)
print("top_logprobs:", r.json()["choices"][0]["logprobs"]["content"][0]["top_logprobs"])
PY

# (b) if (a) printed 5 token/logprob entries, run the pilot
mkdir -p results/probe_decoding_kl
python3 gpu_kit/probe_decoding_kl.py \
    --variants results/runs_real_qwen25_7b_fix/gsm8k_cot_real_qwen25_7b_fix.jsonl \
    --model qwen2.5-14b \
    --base-url http://localhost:8000/v1 \
    --out results/probe_decoding_kl/qwen25_14b_gsm8k_pilot.json

# (c) push results back
git add results/probe_decoding_kl/
git commit -m "pilot: decoding KL probe on Qwen-2.5-14B / GSM8K (n=20)"
git push
```

## What the control PC will do after that push

```bash
git pull
python3 code/analyze_probe_kl.py \
    results/probe_decoding_kl/qwen25_14b_gsm8k_pilot.json
```

Pre-registered pass criteria:
- paired t-test p < 0.01 on `KL_sem - KL_sur`
- ratio mean(KL_sem) / mean(KL_sur) > 1.3x
- |NED_sem - NED_sur| < 0.15 (length confound check)

## Edge cases

1. **vllm both ports dead.** Re-serve with `bash gpu_kit/serve_qwen14b.sh`
   and wait for `Uvicorn running on http://0.0.0.0:8000`.
2. **`top_logprobs` returns empty.** vllm < 0.6.0 doesn't expose it; check
   `pip show vllm` and upgrade to 0.6.x if needed. (We installed 0.6.x in
   `install.sh`.)
3. **Variants jsonl has fewer than 20 questions.** Inspect:
   ```bash
   wc -l results/runs_real_qwen25_7b_fix/gsm8k_cot_real_qwen25_7b_fix.jsonl
   ```
   If < 20, switch `--variants` to e.g. `runs_real_qwen25_3b_fix/...` which
   has 20 rows confirmed.
